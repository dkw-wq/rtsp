# Face detection models

Put SCRFD ONNX models here. The default configuration expects:

```text
models/scrfd_500m.onnx
```

Start with `scrfd_500m.onnx` for the CPU/CUDA pipeline, then try larger SCRFD
variants after the overlay path is stable.
